import { IndividualFollower } from './individual-follower.model';

describe('IndividualFollower', () => {
  it('should create an instance', () => {
    expect(new IndividualFollower()).toBeTruthy();
  });
});
