import { ElementalResistances } from './elemental-resistances.model';

describe('ElementalResistances', () => {
  it('should create an instance', () => {
    expect(new ElementalResistances()).toBeTruthy();
  });
});
