import { CompetitionParty } from './competition-party.model';

describe('CompetitionParty', () => {
  it('should create an instance', () => {
    expect(new CompetitionParty()).toBeTruthy();
  });
});
