import { TrialDefeatCount } from './trial-defeat-count.model';

describe('TrialDefeatCount', () => {
  it('should create an instance', () => {
    expect(new TrialDefeatCount()).toBeTruthy();
  });
});
