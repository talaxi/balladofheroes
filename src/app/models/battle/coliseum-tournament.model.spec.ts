import { ColiseumTournament } from './coliseum-tournament.model';

describe('ColiseumTournament', () => {
  it('should create an instance', () => {
    expect(new ColiseumTournament()).toBeTruthy();
  });
});
