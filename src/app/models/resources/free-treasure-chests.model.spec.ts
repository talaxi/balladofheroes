import { FreeTreasureChests } from './free-treasure-chests.model';

describe('FreeTreasureChests', () => {
  it('should create an instance', () => {
    expect(new FreeTreasureChests()).toBeTruthy();
  });
});
