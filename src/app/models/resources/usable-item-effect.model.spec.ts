import { UsableItemEffect } from './usable-item-effect.model';

describe('UsableItemEffect', () => {
  it('should create an instance', () => {
    expect(new UsableItemEffect()).toBeTruthy();
  });
});
